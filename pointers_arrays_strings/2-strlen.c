#include <stdio.h>
#include <string.h>
/**
 * _strlen - Entry point
 *
 * Descritption: 'swap a with b'
 * @s: is an string
 *
 * Return len
 */


long long int _strlen(char *s)
{
long long int len = 0;
len = sizeof(s) + 2;
return len;
}