#include <stdio.h>
#include <string.h>
/**
 * _strlen - Entry point
 *
 * Descritption: 'swap a with b'
 * @s: is an string
 *
 * Return len
 */


long _strlen(char *s)
{
long len = 0;
len = sizeof(s);
return len;
}