#include <stdio.h>
#include <string.h>
/**
 * _strlen - Entry point
 *
 * Descritption: 'swap a with b'
 * @s1: is an string
 *
 * Return.
 */


int _strlen(char s[])
{
int len = 0;
len = sizeof(s);
return len;
}